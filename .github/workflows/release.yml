name: Build and Release
on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Skip uploading to release'
        type: boolean
        default: true
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: win32
            arch: x64
          - os: macos-latest
            platform: darwin
            arch: x64
          - os: ubuntu-latest
            platform: linux
            arch: x64
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v5
        with:
          path: app
      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
      - name: Install nhyris
        run: npm install -g github:skmonckton/nhyris
      - name: Re-install standalone R
        working-directory: app
        run: npm run install-R
      - name: Debug R_HOME
        if: failure()
        run: |
          export R_HOME="${{ github.workspace }}/app/r-nhyris"
          echo "R_HOME=$R_HOME"
          ls "$R_HOME"
          "$R_HOME/bin/Rscript" --version
      - name: Debug R installation
        if: failure()
        run: |
          ls -la app/r-nhyris/bin/
          file app/r-nhyris/bin/Rscript
          ldd app/r-nhyris/bin/Rscript
      - name: Install dependencies
        run: nhyris update app
      - name: Build app
        run: nhyris build app
      - name: Upload artifacts to GitHub Release
        if: ${{ !inputs.dry_run && startsWith(github.ref, 'refs/tags/') }}
        uses: softprops/action-gh-release@v2
        with:
          files: app/out/make/**/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
